const SUPABASE_URL=process.env.SUPABASE_URL;
const SERVICE_KEY=process.env.SUPABASE_SERVICE_ROLE_KEY;
const SYNC_TOKEN=process.env.IDMS_SYNC_TOKEN;
const CONFIGURED_USER_ID=process.env.KEYS2AUTOSALES_USER_ID;
function headers(){return {apikey:SERVICE_KEY,Authorization:`Bearer ${SERVICE_KEY}`,'Content-Type':'application/json'};}
async function sb(path,options={}){const r=await fetch(`${SUPABASE_URL}/rest/v1/${path}`,{...options,headers:{...headers(),...(options.headers||{})}});const text=await r.text();let data=null;try{data=text?JSON.parse(text):null;}catch{data={message:text};}if(!r.ok)throw new Error(data?.message||data?.hint||`Supabase error ${r.status}`);return data;}
async function resolveUserId(){if(CONFIGURED_USER_ID)return String(CONFIGURED_USER_ID).trim();const r=await fetch(`${SUPABASE_URL}/auth/v1/admin/users?per_page=2`,{headers:{apikey:SERVICE_KEY,Authorization:`Bearer ${SERVICE_KEY}`}});const data=await r.json().catch(()=>({}));const users=Array.isArray(data?.users)?data.users:[];if(users.length===1&&users[0]?.id)return users[0].id;throw new Error('Could not uniquely resolve Keys2AutoSales user.');}
const norm=s=>String(s||'').toLowerCase().replace(/&/g,' and ').replace(/[^a-z0-9]+/g,' ').replace(/\s+/g,' ').trim();
const compact=s=>norm(s).replace(/\s+/g,'');
const num=v=>{const n=Number(String(v??'').replace(/[$,\s]/g,''));return Number.isFinite(n)?n:0;};
const realFacebookUrl=url=>/^https:\/\/(?:www\.)?facebook\.com\/marketplace\//i.test(String(url||''))?String(url):'';
const STOP=new Set(['awd','fwd','rwd','4wd','2wd','automatic','manual','auto','sedan','coupe','wagon','hatchback','hybrid','plug','in','gas','gasoline','flex','fuel','electric','limited','touring','sport','sports','premium','luxury','base']);
const TOKEN_ALIASES={chevy:'chevrolet',vw:'volkswagen',merc:'mercedes',benz:'mercedes'};
function canonToken(t){t=String(t||'').toLowerCase();return TOKEN_ALIASES[t]||t;}
function tokens(s){return norm(s).split(' ').map(canonToken).filter(Boolean);}
function coreModel(modelTrim){const raw=String(modelTrim||'').trim();return norm((raw.split(/[\/|]/)[0]||raw));}
function trimText(modelTrim){const raw=String(modelTrim||'').trim();return norm(raw.split(/[\/|]/).slice(1).join(' '));}
function parseListingIdentity(listing){const raw=`${listing.title||''} ${listing.raw_text||''}`;const text=norm(raw),title=norm(listing.title||'');const y=(title||text).match(/\b(19\d{2}|20\d{2})\b/);return {text,title,compact:compact(text),year:y?Number(y[1]):0,price:num(listing.price),mileage:num(listing.mileage),tokens:new Set(tokens(text))};}
function makeScore(identity,v){const raw=norm(v.make);if(!raw)return {score:0,reason:'no make'};const ts=tokens(raw),c=compact(raw);if(c&&identity.compact.includes(c))return {score:22,reason:`make ${v.make}`};if(ts.length&&ts.every(t=>identity.tokens.has(t)))return {score:22,reason:`make ${v.make}`};if(ts.length===2&&identity.tokens.has(ts[0]))return {score:12,reason:`partial make ${v.make}`};return {score:0,reason:`make ${v.make} not found`};}
function modelScore(identity,v){const model=coreModel(v.model_trim);if(!model)return {score:0,reason:'no model'};const mt=tokens(model).filter(t=>!STOP.has(t)),mc=compact(model),hit=mt.filter(t=>identity.tokens.has(t)).length,ratio=mt.length?hit/mt.length:0;if(mc&&identity.compact.includes(mc))return {score:35,reason:`core model ${model}`};if(mt.length===1&&identity.tokens.has(mt[0]))return {score:35,reason:`core model ${model}`};if(ratio===1&&mt.length)return {score:35,reason:`core model ${model}`};if(ratio>=0.67&&hit>=1)return {score:25,reason:`partial model ${model}`};return {score:0,reason:`model ${model} not found`};}
function priceScore(identity,v){const lp=identity.price,vp=num(v.fb_price)||num(v.dealer_price);if(!lp||!vp)return {score:0,diff:null};const diff=Math.abs(lp-vp),pct=diff/Math.max(vp,1);if(diff===0)return {score:12,diff};if(diff<=500||pct<=.03)return {score:10,diff};if(diff<=1000||pct<=.06)return {score:7,diff};if(diff<=2000||pct<=.12)return {score:3,diff};return {score:-7,diff};}
function mileageScore(identity,v){const lm=num(identity.mileage),vm=num(v.mileage);if(!lm||!vm)return {score:0,diff:null};const diff=Math.abs(lm-vm),pct=diff/Math.max(vm,1);if(diff<=100)return {score:15,diff};if(diff<=500||pct<=.01)return {score:13,diff};if(diff<=1500||pct<=.025)return {score:10,diff};if(diff<=3500||pct<=.05)return {score:6,diff};if(diff<=7500||pct<=.10)return {score:2,diff};return {score:-10,diff};}
function scoreVehicle(identity,v){let score=0;const why=[];const vy=Number(v.year)||0;if(identity.year&&vy===identity.year){score+=28;why.push('year');}else if(identity.year&&vy&&vy!==identity.year)return {score:-100,why:['year mismatch']};const mk=makeScore(identity,v);if(!mk.score)return {score:-80,why:[mk.reason]};score+=mk.score;why.push(mk.reason);const ms=modelScore(identity,v);if(!ms.score)return {score:-60,why:[ms.reason]};score+=ms.score;why.push(ms.reason);const trim=trimText(v.model_trim);if(trim&&tokens(trim).filter(t=>!STOP.has(t)).some(t=>identity.tokens.has(t))){score+=4;why.push('trim');}const ps=priceScore(identity,v);score+=ps.score;if(ps.score>0)why.push(ps.diff===0?'exact price':'close price');else if(ps.score<0)why.push('price differs');const ml=mileageScore(identity,v);score+=ml.score;if(ml.score>0)why.push(ml.diff<=100?'near-exact mileage':'close mileage');else if(ml.score<0)why.push('mileage differs');const inv=norm(v.inventory_status);if(/available|active|in stock/.test(inv)){score+=3;why.push('available');}else if(/sold|removed|unavailable/.test(inv)){score-=20;why.push('not available');}return {score:Math.max(0,Math.min(100,score)),why,price_diff:ps.diff,mileage_diff:ml.diff};}
function matchListing(listing,vehicles){const identity=parseListingIdentity(listing);if(!identity.year)return {vehicle:null,status:'unmatched',confidence:0,reason:'listing identity missing year',alternatives:[]};const ranked=vehicles.map(v=>({vehicle:v,...scoreVehicle(identity,v)})).filter(x=>x.score>0).sort((a,b)=>b.score-a.score||((a.mileage_diff??Infinity)-(b.mileage_diff??Infinity))||((a.price_diff??Infinity)-(b.price_diff??Infinity)));const best=ranked[0],second=ranked[1];if(!best)return {vehicle:null,status:'unmatched',confidence:0,reason:'no year/make/model inventory match',alternatives:[]};const gap=best.score-(second?.score||0),reason=`${best.score}% confidence: ${best.why.join(' + ')}`;const alternatives=ranked.slice(0,3).map(x=>({vehicle_id:x.vehicle.id,stock_number:x.vehicle.stock_number,year:x.vehicle.year,make:x.vehicle.make,model_trim:x.vehicle.model_trim,mileage:x.vehicle.mileage,dealer_price:x.vehicle.dealer_price,score:x.score,mileage_diff:x.mileage_diff,price_diff:x.price_diff}));if(best.score>=85&&(gap>=7||!second))return {vehicle:best.vehicle,status:'matched',confidence:best.score,reason,alternatives};if(best.score>=70)return {vehicle:null,status:'ambiguous',confidence:best.score,reason:`review required — ${reason}${second?`; next best ${second.score}%`:''}`,alternatives};return {vehicle:null,status:'unmatched',confidence:best.score,reason:`low confidence — ${reason}`,alternatives};}
function protectDuplicateVehicleClaims(evaluated){const groups=new Map();for(const row of evaluated){if(row.match.status!=='matched'||!row.match.vehicle?.id)continue;const id=row.match.vehicle.id;if(!groups.has(id))groups.set(id,[]);groups.get(id).push(row);}let collisionGroups=0,collisionListings=0;for(const rows of groups.values()){if(rows.length<2)continue;collisionGroups++;collisionListings+=rows.length;for(const row of rows)row.match={...row.match,vehicle:null,status:'ambiguous',reason:`duplicate Marketplace collision — ${rows.length} listings point to the same inventory vehicle; manual review required`};}return {collisionGroups,collisionListings};}
export default async function handler(req,res){if(req.method!=='POST'){res.setHeader('Allow','POST');return res.status(405).json({error:'Method not allowed'});}try{if(!SUPABASE_URL||!SERVICE_KEY)return res.status(503).json({error:'Missing Supabase server environment variables.'});if(!SYNC_TOKEN)return res.status(503).json({error:'IDMS_SYNC_TOKEN is not configured.'});if(String(req.headers.authorization||'')!==`Bearer ${SYNC_TOKEN}`)return res.status(401).json({error:'Invalid sync token'});const listings=Array.isArray(req.body?.listings)?req.body.listings:[],dryRun=Boolean(req.body?.dry_run);if(!listings.length)return res.status(400).json({error:'No Facebook Marketplace listings received.'});const userId=await resolveUserId(),now=new Date().toISOString();const vehicles=await sb('vehicles?select=id,stock_number,vin,year,make,model_trim,dealer_price,fb_price,mileage,fb_posted,fb_listing_url,fb_status,inventory_status');const evaluated=listings.map(listing=>({listing,match:matchListing(listing,vehicles)})),collisions=protectDuplicateVehicleClaims(evaluated);let matched=0,ambiguous=0,unmatched=0,withRealUrl=0;const results=[];for(const row of evaluated){const listing=row.listing,m=row.match;if(m.status==='matched')matched++;else if(m.status==='ambiguous')ambiguous++;else unmatched++;const captureUrl=String(listing.listing_url||listing.actual_listing_url||`k2fb://capture/${listing.listing_id||Date.now()}`),actualUrl=realFacebookUrl(listing.actual_listing_url||listing.listing_url);if(actualUrl)withRealUrl++;const capture={user_id:userId,captured_at:now,listing_id:String(listing.listing_id||''),listing_url:captureUrl,title:String(listing.title||''),price:num(listing.price)||null,status:String(listing.status||'active'),raw_text:String(listing.raw_text||'').slice(0,1200),matched_vehicle_id:m.vehicle?.id||null,match_status:m.status,match_reason:m.reason};if(!dryRun){await sb('marketplace_listing_captures?on_conflict=user_id,listing_url',{method:'POST',headers:{Prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify(capture)});if(m.status==='matched'&&m.vehicle){const active=!['sold','draft'].includes(String(listing.status||'').toLowerCase());const patch={fb_posted:active,fb_price:num(listing.price)||m.vehicle.fb_price||null,fb_status:active?'LIVE':'VERIFY',fb_last_verified_at:now,updated_at:now};if(actualUrl)patch.fb_listing_url=actualUrl;await sb(`vehicles?id=eq.${encodeURIComponent(m.vehicle.id)}`,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify(patch)});}}results.push({listing_id:listing.listing_id||'',title:listing.title||'',price:num(listing.price)||0,mileage:num(listing.mileage)||0,status:m.status,confidence:m.confidence||0,reason:m.reason,vehicle_id:m.vehicle?.id||null,stock_number:m.vehicle?.stock_number||null,vin:m.vehicle?.vin||null,has_real_url:Boolean(actualUrl),alternatives:m.alternatives||[]});}const confidence={high:results.filter(x=>x.status==='matched'&&x.confidence>=85).length,review:results.filter(x=>x.status==='ambiguous').length,low:results.filter(x=>x.status==='unmatched').length};return res.status(200).json({ok:true,dry_run:dryRun,total:listings.length,matched,ambiguous,unmatched,with_real_url:withRealUrl,without_real_url:listings.length-withRealUrl,with_mileage:results.filter(x=>x.mileage>0).length,confidence,duplicate_collision_groups:collisions.collisionGroups,duplicate_collision_listings:collisions.collisionListings,results});}catch(err){console.error('Marketplace reconcile failed',err);return res.status(500).json({error:err.message||'Marketplace reconcile failed'});}}
